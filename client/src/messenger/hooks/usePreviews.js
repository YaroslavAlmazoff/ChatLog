import { useState, useEffect, useCallback } from "react";

export default function usePreviews(
  files,
  setFiles,
  initialState,
  messageFieldRef,
  setCanChooseImage,
  setCanChooseVideo,
  setFilesVisible
) {}
