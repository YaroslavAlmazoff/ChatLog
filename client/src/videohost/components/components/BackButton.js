const BackButton = () => {
    return (
        <button onClick={() => window.location = '/videohost'} className="videohost-button" style={{marginLeft: '15px'}}>На главную</button>
    )
}

export default BackButton